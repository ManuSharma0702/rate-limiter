[package]
name = "rate-limiter"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
axum = "0.8.8"
redis = { version = "1.0.2", features = ["tokio-comp"] }
tokio = { version = "1", features = ["full"] }
